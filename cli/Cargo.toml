[package]
name = "kiwami"
version = "0.1.0"
edition = "2021"
description = "Kiwami desktop control CLI"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
